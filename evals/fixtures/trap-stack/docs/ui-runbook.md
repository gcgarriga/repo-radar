# UI Runbook

The data team must click through the provider portals every Friday and copy the exported files into the shared folder. If a row looks wrong, fix it in the spreadsheet before running the pipeline.
